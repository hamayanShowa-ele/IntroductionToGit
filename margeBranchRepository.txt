リポジトリのブランチのマージ

マージとは、簡単に言えば「統合する」となる。つまりブランチ同士の統合となる。

(1) リポジトリの現行ブランチをmasterに戻す。(branchRepository.txtを参照)

(2) SourceTreeの左側のdebugブランチの文字の上で右クリックしてメニューを出す。

(3) メニューの上から２番目の「現在のブランチにdebugをマージ」をクリックする。

(4) 確認のメッセージボックスが出るが、気にせず進む。

フォルダーにtest1.txtとtest2.txtができている事を確認する。
